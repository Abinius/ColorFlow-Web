flask>=3.0.0
mcp-print>=0.1.0
colorflow-sdk @ git+https://github.com/Abinius/ColorFlow.git
fastmcp>=2.0.0
# 抠图功能（Web 端「抠图 (Cutout)」模式）；国内安装可加 -i https://pypi.tuna.tsinghua.edu.cn/simple
rembg[cpu]>=2.0.0
# 印刷 PDF 导出（export_print 需要 reportlab + svglib 渲染 SVG→PDF）
svglib>=1.5.0
reportlab>=4.0.0
